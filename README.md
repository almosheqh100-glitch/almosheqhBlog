# almosheqhBlog

تطبيق أندرويد لمدونة عبدالرحمن المشيقح — الإصدار 1.1.0.

## التنزيل

نزّل ملف **almosheqhBlog-1.1.0.apk** من [صفحة الإصدارات](https://github.com/almosheqh100-glitch/almosheqhBlog/releases).
الإصدار النهائي هو Release APK موقّع بمفتاح إصدار، وليس مفتاح Debug.
إذا ثبّتَّ النسخة التجريبية السابقة، احذفها أولًا بسبب اختلاف التوقيع، ثم ثبّت النسخة الجديدة.
الإصدار متاح للتثبيت المباشر؛ لم يُنشر في Google Play.

## الصفحة الرئيسية

تظهر التصنيفات مباشرة كأزرار، مع تحديث أعدادها من المدونة:

- المقالات (185)
- البرامج (17)
- تطبيقات Android (2)
- تطبيقات windos (2)
- تطبيقات الآيفون (17)

هذه الأعداد كانت صحيحة عند تجهيز الإصدار، وتتغير تلقائيًا بحسب المدونة. تُعرض آخر نسخة محفوظة عند تعذر الاتصال مع توضيح ذلك.

اضغط على التصنيف لعرض مقالاته، ثم «تحميل المزيد» لعرض المقالات الأقدم. البحث من الرئيسية يشمل المدونة، ومن داخل التصنيف يقتصر عليه. يتوفر زر للرجوع إلى التصنيفات وقراءة المقال وفتحه في المتصفح.

روابط التواصل:
- [فيسبوك](https://www.facebook.com/profile.php?id=61565536892612)
- [تويتر (X)](https://x.com/almosheqh19Aa)

## الإشعارات

يفحص التطبيق المقالات الجديدة كل 30 دقيقة أثناء تشغيله فقط، ويتطلب السماح بالإشعارات. لا توجد خدمة إشعارات فورية بعد إغلاق التطبيق. لم تُختبر الإشعارات أو TalkBack على جهاز فعلي.

## البناء والتحقق

يتطلب Python 3.13 وJava 17 وAndroid SDK:

```sh
python -m pip install briefcase==0.4.5 httpx pytest toga-dummy==0.5.6
python -m unittest discover -s tests -v
briefcase create android --no-input
briefcase build android --no-input
briefcase package android -p apk --no-input
```

ينفّذ GitHub Actions الاختبارات ويبني Release APK غير موقع. ملف Actions يحتاج توقيعًا قبل تثبيته؛ استخدم ملف APK الموقع المنشور في Releases للتثبيت المباشر.
يُحفظ مفتاح الإصدار وكلمة مروره محليًا لدى المالك، ولا يُرفعان إلى المستودع. يجب استخدام المفتاح نفسه عند إصدار التحديثات.

لم يتضمن المصدر ترخيص توزيع؛ لا يمنح نشر المستودع ترخيصًا مفتوح المصدر تلقائيًا.
